APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=pendelordner
DB_USERNAME=root
DB_PASSWORD=

# --- OCR (Tesseract) ---
TESSERACT_PATH="C:/Program Files/Tesseract-OCR/tesseract.exe"
TESSERACT_LANG=deu

# --- Belegarchiv ---
BELEGE_DISK=belege

# --- FinTS / automatischer Bankabruf ---
FINTS_PRODUCT_REGISTRATION=
FINTS_PRODUCT_VERSION=1.0
# Uhrzeit des täglichen automatischen Abrufs (HH:MM)
BANK_FETCH_TIME=06:00

# --- Rechnungseingang per E-Mail (IMAP) ---
# Postfach (z. B. belege@deinedomain.de), an das du deine Rechnungs-Mails
# weiterleitest. Abruf alle 15 Min per Scheduler (belege:fetch-mail).
MAIL_INGEST_ENABLED=false
MAIL_INGEST_HOST=
MAIL_INGEST_PORT=993
MAIL_INGEST_ENCRYPTION=ssl
MAIL_INGEST_VALIDATE_CERT=true
MAIL_INGEST_USERNAME=
MAIL_INGEST_PASSWORD=
MAIL_INGEST_FOLDER=INBOX
# Verarbeitete Mails in diesen Ordner verschieben (leer = nur als gelesen markieren)
MAIL_INGEST_PROCESSED_FOLDER=
# Betrieb-ID, der neue Mail-Belege zugeordnet werden (leer = keiner)
MAIL_INGEST_BUSINESS_ID=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
